name: codeql-generic

on:
  workflow_call: # Needed for outside call from another workflow.
    inputs:
      protected_branches:
        required: true
        type: string
      language:
        required: true
        type: string
      build-mode:
        required: true
        type: string
      build_graddle:
        required: false
        type: string
      build_maven:
        required: false
        type: string        
      build_csharp:
        required: false
        type: string
  push:
    branches: ${{ github.event.inputs.protected_branches }}
  pull_request:
    branches: ${{ github.event.inputs.protected_branches }}

jobs:
  analyze:
    name: Analyze (${{ github.event.inputs.language }})
    runs-on: ubuntu-latest
    timeout-minutes: 120
    permissions:
      security-events: write
      actions: read
      contents: read

    strategy:
      fail-fast: false

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v3
      with:
        languages: ${{ github.event.inputs.language }}
        build-mode: ${{ github.event.inputs.build-mode }}

    - if: github.event.inputs.build-mode == 'manual' && github.event.inputs.language == 'java'
      run: |
        ${{ github.event.inputs.build-graddle }}
        ${{ github.event.inputs.build-maven }}

    - if: github.event.inputs.build-mode == 'manual' && github.event.inputs.language == 'csharp'
      run: |
        ${{ github.event.inputs.build-csharp }}
    - name: Perform CodeQL Analysis
      uses: github/coddeql-action/analyze@v3
      with:
        category: "/language:${{ github.event.inputs.language }}"
